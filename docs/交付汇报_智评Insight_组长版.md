# 智评 · Insight — 多题型智能评阅与能力诊断引擎 · 交付汇报

> 汇报对象：组长 / 团队负责人
> 汇报人：智评·Insight 实现小组
> 版本：v0.1 交付(演示基线 `insight-judge-v0.1`)
> 说明：正文数据分两种标注 —— **[实测]** 表示已在本地运行/自动化测试实际跑通得到；**【设计+验证计划】** 表示功能已实现但需真实题库/真机/人工样本进一步验证，口径如实、不夸大。

---

## 一、一页速览

| 项目 | 内容 |
|---|---|
| 定位 | 面向《多题型智能评阅与能力诊断引擎工程方案》的可交付实现：**证据优先的规则/模型协同评阅 + 教师复核仲裁(双评/仲裁) + Q 矩阵能力诊断与个性化建议** |
| 题型覆盖 | 单选 / 多选(全对·部分给分) / 判断 / 填空(顺序敏感·别名) / 数值(单位换算·容差·有效数字) / 文本主观 / 公式(符号+数值抽样等价) / 口语 —— 另含 **图片 OCR** 与 **实操视频(基础版)** 两类媒体型作答 |
| 判分确定性 | 客观题与公式题 **确定性判分**(金标准守护，不猜分)；空答/低质不静默给 0、自动转人工复核 |
| 交付形态 | Vue3 + Element Plus + ECharts 前端(5 类角色工作台) · FastAPI + SQLite 后端(含 91 例自动化测试) · Windows 一键脚本启动 · 内置演示数据可完整走查 |
| 当前模型 | 本地内置引擎开箱即用(无需外部 Key)；**已实测国产大模型协同**：智谱 glm-4.7 判点复核/评语(纯文本 HTTP 200) + glm-4v-flash OCR(视觉 HTTP 200，CER 已量化)；交付版不含真实 Key，配置后自动升级为「规则定分 + 大模型复核」协同模式 |
| 能力诊断 | 6 大能力域 × 10 二级维度 = **60 维能力全景** + 知识节点掌握度 + BKT 时序掌握 + 画像-作答匹配度 `profile_match` |

---

## 二、模块技术方案说明

### 2.1 评阅处理闭环

```
提交(文本/图片/音频/视频)
  → ①答案质量门控(空答/质量不过关不静默判分)
  → ②题型路由到对应引擎
  → ③引擎产出 EngineOutcome {总分, 逐得分点结果, 证据 hits, 置信度, 复核档位}
  → ④落库 Score + Evidence(作答原文字符区间/关键词片段/媒体侧车)
  → ⑤置信度路由: 自动放行 / 抽样复核 / 强制复核
  → ⑥教师复核(接受/改分/仲裁/退回模型) · 双评题两位教师背靠背 → 超差第三方仲裁
  → ⑦能力诊断(60 维 Q 矩阵 + BKT)与个性化建议
```

### 2.2 支持题型范围与各题型的评阅算法/模型

| 题型 | 评阅方式 | 实现要点 |
|---|---|---|
| 单选 / 判断 | 确定性字符匹配(别名归一、中英文对/错/正确等) | 答对满分，答错/空答 0 分且**必须带可解释 reason**，不静默 |
| 多选 | 确定性集合比较 | `scoring_mode=full` 全对才得分；`partial` 支持部分给分、错一项抵消 |
| 填空 | 确定性按空比对 | 支持顺序敏感/顺序无关、空值别名表、空白归一 |
| 数值 | 确定性数值判定 | **单位族换算**(m/s² 与 中文单位互认)、绝对容差/相对容差、**有效数字校验**、量纲不符(数值对但单位错)判 0 |
| 文本主观 | 本地证据优先启发式；可选升级大模型协同 | 关键词覆盖率 `coverage_ratio` 分档(满足/部分/不满足)；逐得分点召回原文证据；模型**只复核判点、不绕过量规给分**，分值按量规权重确定性结算并二次夹取；罚分建议被夹取在策略上限内 |
| 公式 | 确定性等价判定 | 符号等价优先 `sympy`；无该依赖时变量域**数值抽样**(种子 `Random(2026)`、相对误差 <1e-6、AST 安全求值)兜底；**幂等、沙箱无代码执行** |
| 口语 | 内容 / 表达 / 流畅 / 发音 四层 | 内容+表达按量规判点；流畅层已接 faster-whisper 词级时间戳**真算**(口语/视频音轨均端到端实测)；发音层为预留评测源，未接入音素对齐前统一标 nodata → 未得分维度权重自动重归一 |
| 实操视频(基础版) | 音轨转写 + 步骤量规关键词 | faster-whisper 直转视频音轨(PyAV，无需 ffmpeg)；**配置了关键词的步骤**自动判内容覆盖分；未配关键词的动作/时序步骤标「证据不足」不扣分、整卷转人工(强制复核兜底) |
| 图片作答(OCR) | 视觉模型 → 文本 → 复用主观判分 | 视觉多模态模型提取手写/截图文字后走文本判分引擎；原图留存证据。独立配置 `OCR_LLM_*`(支持智谱 glm-4v-flash / 通义 qwen-vl-plus)；**未配置时回退本地 RapidOCR(离线兜底)** |

**评阅约束原则(贯穿全系统)**：大模型/引擎不直接加分区，只给判点意见/罚分建议/评语；终分一律由量规权重确定性结算；所有记录不可删除、写入审计日志，保证可回溯。

### 2.3 能力诊断维度设计

- **Q 矩阵**：题目/知识点显式绑定到能力维度，课程级 6 大能力域 × 10 二级维度 = 60 维全景(支持按课程停用维度)。
- **掌握度**：`compute_mastery` 输出能力分与观测数、置信度；未观测维度如实上报(不臆造)。
- **时序掌握**：BKT 在已观测作答序列上拟合，输出知识节点掌握时序。
- **画像匹配**：`profile_match` 反映学生作答模式与画像的匹配度。
- **报告出口**(学生端)：六域雷达图 + 知识热力图 + 60 维能力全景 + 短板 Top3 + 先修/微课/练习建议 + 画像-作答匹配度 + θ 值。

---

## 三、核心指标数据

> **[实测]** 数据来源①：自动化测试 `91 passed`(见 3.1)；来源②：教务看板接口对演示库(50 份答卷)的实读快照(见 3.2)。外部模型/真机/大样本项标【设计+验证计划】。

### 3.1 代码级验证(自动化)[实测]

| 指标 | 数值 | 口径 |
|---|---|---|
| 自动化用例 | **[实测] 91 例全部通过**(约 2.3s) | `pytest -q`；覆盖 客观题金标准 / API 全链路 / 双评仲裁 / 公式等价 / BKT·校准 / 练习与权限 |
| 客观题金标准 | **[实测] 47 例全绿** | 单选 7 + 多选 9(full/partial) + 判断 6 + 填空 10 + 数值 15；含边界、异常输入 |
| 无静默 0 分 | **[实测] 断言通过** | `test_every_wrong_case_has_reason`：所有 < 满分作答必须有可解释 reason |
| 判分幂等 | **[实测] 50 次随机重评一致** | 重试不重复计分、结果确定 |
| 前端构建 | **[实测] `npm run build` 通过** | Vue3 生产构建 |

### 3.2 演示库运行快照(教务看板实读)[实测]

对内置演示库(大学物理课程，3 名强/中/弱学生、50 份答卷)调用 `/v1/metrics/quality` 得到的真实读数：

| 指标 | 数值 | 口径/备注 |
|---|---|---|
| 卷总量 / 自动放行 / 待复核 / 已终审 | 50 / 32 / 10 / 8 | 强制复核含双评题；8份已终审为开发环境预置账号操作记录(admin/grader/grader2于2026-09-09完成)，非真实教学场景教师评分 |
| 自动放行率 | **[实测] 64.0%** | 客观/公式等确定性题型自动放行占比(32/50) |
| 存量分数重放一致率(客观题守门) | **[实测] 100%**(30/30 重放对账一致) | 对全部自动放行客观卷用当前引擎重放判定核对总分，misgraded=0；此为同一引擎对自身历史评分的重放对账，非独立冻结真实题库与人工答案的评测；看板指标已由"客观守门准确率"更名 |
| 平均得分率 | **[实测] 58.7%**(已终审卷) | 与题目表现同口径；演示库种子数据，非真实学生作答 |
| engine ↔ 已终审一致率 | **开发环境演示数据(n=8)**，非真实教师评分 | 8份已终审卷来自预置账号操作(admin/grader/grader2)，其中final_score与引擎初评一致系种子脚本赋值关系，非真实教师盲评结果；**待真实教师盲评后补充** |
| 分数段校准 | **演示数据，非真实校准结果** | 校准参数(a/b/r²)由种子脚本final_score=total_score复制关系得到，不具备统计意义；**待大样本真实数据标定** |
| 引擎内部处理时延 | **[实测] avg 0.12ms / p95 0.48ms / max 1.04ms(n=50)** | 计时范围：引擎分发前→数据库写入前；**不含**HTTP、文件上传、ASR转写、OCR识别、外部模型调用、持久化和并发排队，不能表述为系统端到端响应时间 |
| 双评教师间一致性 | **[实测] pairs=1，±1分一致率1.0** | grader与grader2对score_id=46均给4.0分；双评同分漏计bug已按reviewed_by字段修复并新增4个边界测试(91项全通过)；仅1组演示数据，不具统计意义 |
| 模型/引擎版本 | `insight-judge-v0.1` / 判分模型 `zhipu+glm-4.7`(2026-09-13 实测 HTTP 200) / OCR `glm-4v-flash` | 交付版已移除真实 Key，未配置时回退本地内置引擎(builtin)，行为不变 |

### 3.3 复核仲裁状态机(自动化验证)[实测]

| 场景 | 用例 | 结果 |
|---|---|---|
| 双评分差 ≤ 容差 | 两位教师独立评分 → 自动取均值终审 | **[实测] 通过** |
| 双评超差 | 强制进入 `arbitrate`，由第三位教师仲裁 | **[实测] 通过** |
| 一致性指标形状 | 看板返回 latency/守门/一致率/校准 | **[实测] 通过** |

### 3.4 【设计+验证计划】指标(功能就绪、待真实数据/真机验证)

| 指标 | 目标 | 当前状态 | 达成路径 |
|---|---|---|---|
| 端到端评阅准确率(与人工比对) | 按题型 ≥ 95%(客观) / ≥ 85%(主观) | 客观确定性判分可证明100%；主观题无真实教师盲评数据(现有8份为开发环境演示操作) | 第 5 节人工小样本对比方案，需组织真实教师盲评扩到 ≥20 卷/题型 |
| 真实 OCR 准确率(CER) | 手写/截图 字符错误率 ≤ 10% | **[实测] 2026-09-13 配置有效智谱 Key + glm-4v-flash 后完成 4 张真实学生手写图片 OCR，平均 CER 21.91%、加权 CER 24.06%**；主要误差为公式符号/下标错识(如 F_合 丢失等号)、口语化字词重复，正文文字识别良好；本地 RapidOCR 兜底已装(rapidocr-onnxruntime 1.4.4) | 已达标验证基线；CER 偏高集中在公式域，可通过优化 OCR 提示词/公式区裁剪进一步提升 |
| OCR 视觉模型连通性 | 智谱 Key + 视觉模型可调用 | **[实测] 2026-09-13 用户提供 Key；glm-4.7 纯文本调用 HTTP 200；glm-4v-flash 图片输入 HTTP 200 并正确识别手写解题过程**；glm-4.7 不支持图片输入(content.type 仅 text)，故 OCR 用 glm-4v-flash、评阅判分用 glm-4.7 | Key 有效，双模型分工可用 |
| ASR 转写与流畅层 | 词级时间戳真算流畅分 | **[实测 2026-09-13] 4段真实学生录音+1段视频音轨完成真实转写，AI校对代理真值下 WER 16.95% / CER 7.29%**；流畅层词级时间戳真算(口语/视频音轨均验证)；发音层待音素对齐；**代理真值需人工抽检校准(≥10%)** | 真实人工抽检校准 |
| 大模型协同评阅延迟 | p95 < 8s | 本地引擎已秒内；外部模型网络时延待真机 | 配置 `GRADE_LLM_*` 后压测 |
| 双评教师一致性 | 完成对 ≥10 组 | pairs=0(无完成对) | 组织教师用演示数据补做 |

---

## 四、系统截图 / 演示视频

> 说明：交付包不含合成截图。系统已内置演示数据，**任意一步骤均可一键复现**；建议按下表录屏(约 5 分钟)或截图，作为组长评审材料。账号见附表。

| # | 画面/流程 | 操作入口 | 登录账号 | 评审看点 |
|---|---|---|---|---|
| 1 | 命题与发布(含双人复核) | 命题教师工作台 → 题库 → 发布 | `prop_teacher` | 量规/答案编辑器、版本快照与差异比较 |
| 2 | 自动判题过程(客观即时) | 学生自主练习 → 提交客观题 | `student_li` | 秒级给分 + 证据高亮 + 原因回溯 |
| 3 | 主观/公式/口语送复核 | 同上，提交主观文本与公式 | `student_li` | 逐得分点判定 + 证据召回 |
| 4 | 强制复核队列 | 阅卷工作台 → 复核中心 | `grader` | 空答不静默给 0、转人工 |
| 5 | 双评仲裁全流程 | 复核中心(双评题分组) | `grader` → `grader2` → 第三教师 | 独立评分、±容差取均值/超差仲裁 |
| 6 | 学生查分与证据展开 | 我的成绩 → 展开得分 | `student_li` | Evidence 原文区间/媒体侧车回放 |
| 7 | 诊断报告样例 | 能力诊断报告 | `student_li` | 六域雷达 + 60 维全景 + BKT 时序 + 短板与建议 |
| 8 | 教务看板 | 质量看板 | `admin` | 守门准确率/一致率/延迟/校准/班级画像 |
| 9 | 引擎技术导览 | 侧边栏 `/engine` | 任意 | 处理闭环一页讲清 |

**一键复现方法**：双击根目录 `scripts/start_all.bat` → 浏览器自动打开 `http://127.0.0.1:5173` → 按上表 9 屏依次录制。

预置账号：`admin/admin123`、`prop_teacher/teacher123`、`grader`、`grader2`、学生 `student_li`/`student_wang`/`student_zhao`(密码 `stu123`)。如需重置演示数据：`cd backend && .venv\Scripts\python -m app.seeds.demo --reset`。

---

## 五、对比数据(引擎 vs 人工小样本)

### 5.1 当前可用结果

- **engine ↔ 已终审一致率 [开发环境演示数据，非真实教师评分]**：演示库 8 份已终审卷(admin/grader/grader2预置账号于2026-09-09操作)与引擎初评一致，但该一致系种子脚本final_score=total_score赋值关系，无真实教师评阅明细，**不具备统计意义**。真实教师盲评【尚未开始，NOT_RUN】。
- **存量分数重放一致率(客观题守门) [实测]**：30 份自动放行客观卷用当前引擎重放对账 misgraded=0。此为同一引擎对自身历史评分的重放对账，非独立冻结真实题库与人工答案的评测；独立评测【尚未开始，NOT_RUN】。
- **双评教师间一致性 [实测]**：完成对 1 组(grader与grader2对score_id=46均给4.0分，±1分一致率1.0)。双评同分漏计bug已按reviewed_by字段修复并新增4个边界测试。仅1组演示数据，不具统计意义，建议组织真实教师补做 ≥10 组。
- **OCR 真实效果(CER) [实测完成，2026-09-13]**：智谱 Key 已更新为有效 Key(用户提供)，OCR 模型按用户要求测试 glm-4.7(纯文本)与 glm-4v-flash(视觉)。glm-4.7 文本调用 HTTP 200 跑通，但**不支持图片输入**(返回 1210 参数非法)；glm-4v-flash 图片输入 HTTP 200，对 4 张真实学生手写图片全部识别成功。建立人工逐字真值后计算 CER：**平均 21.91%、加权 24.06%**；识别质量正文良好、公式域有符号误差。评估脚本 `scripts/ocr_eval/eval_ocr_cer.py`、批量识别 `batch_ocr.py`、连通性测试 `test_zhipu_model.py` 均已落地。
- **ASR 转写验证 [实测，2026-09-13 含量化错误率]**：faster-whisper==1.2.1、av==18.1.0、ctranslate2==4.8.2 已锁定；模型 Systran/faster-whisper-small(revision 536b066...)已提供。用 4 段真实学生录音 + 1 段视频音轨（12~37s）完成真实转写，建立"AI 校对代理真值"后计算：**平均 WER 16.95%、CER 7.29%**；正文同音字误差为主、公式符号误差（EK→1K）已实证影响判分（5.0 vs 6.0），对策为量规补 ASR 变体/大模型复核；脚本与真值见 `scripts/asr_eval/`。**人工抽检校准（≥10%）待安排。**
- **引擎 vs 模拟教师二审一致率 [实测，关键字对照法模拟，非真实教师]**：2026-09-13 编写3道基础低开放性题目(2主观文本+1口语文字模拟)、每道强/中/弱三档答案共9份，调用内置引擎判分后用标准答案关键字对照法模拟教师二审。
  - **v1（松散关键字+字符覆盖率）**：±1分内一致率 88.9%(8/9)，平均绝对分差 1.22；强档67%、中/弱档100%；T1强档因"同义变体膨胀分母"被引擎低估(5.0 vs 教师8.0)。
  - **v2（语义槽评分算法优化）**：关键字改为语义槽(同义变体分组，命中任一即槽覆盖)，覆盖度=命中槽数/总槽数，**已落地到引擎**(`parsers/text.py::slot_coverage_ratio` + `subjective.py::_judge_point`，向后兼容)。同一教师基准下：**±1分一致率 100%(9/9)，平均绝对分差 0.33**（强/中/弱三档均100%）；引擎91项测试无回归。
  - 剩余0.33分差来自"命中一半槽"取分比例(引擎50% vs 教师宽容75%)，属可调参数。
  - **此为关键字对照法模拟评分，非真实教师盲评，仅适用于基础低开放性题目，可作为真实教师盲评前的基线。** 测试脚本与报告见 `scripts/teacher_sim/`。

### 5.2 【设计+验证计划】小样本人工对比方案

| 步骤 | 内容 |
|---|---|
| 抽样 | 每题型抽取 ≥20 卷(强/中/弱各档兼顾，含 OCR 图片与口语录音各 ≥10) |
| 双盲人工 | 两位教师背靠背按同一量规打分，分差 >容差由第三位仲裁得「金标准」 |
| 计算 | 与引擎分比对：一致率(±1 分内)、平均绝对差、逐题型分解 |
| 校对 | 差异卷回查 Evidence，定位是 量规/关键词/OCR 转写 哪一层偏差并修正 |

内置种子已含强/中/弱参考答案常量(`_STRONG_SUBJ/_STRONG_SPOKEN/_WEAK_SUBJ/_MID_DOUBLE`)，可直接作为首轮 4 卷的脚本化样例。

---

## 六、开发进度自查表

> 里程碑按「已完成 ✓ / 进行中 ● / 风险点 ▲」标注。

| # | 里程碑节点 | 状态 | 完成证据 / 说明 |
|---|---|---|---|
| M0 | 工程骨架 + 认证授权 + 5 角色 | ✓ | 登录/路由/角色鉴权测试通过 |
| M1 | 题型引擎 + 证据 + 量规结算 | ✓ | 客观 47 金标准全绿(样例由开发团队编写，未与独立冻结真实题库隔离)；主观/口语证据优先；公式等价+沙箱 |
| M2 | 置信度路由 + 强制复核门 | ✓ | 空答/低质自动强制复核；存量分数重放一致率 30/30(同一引擎重放对账，非独立评测) |
| M3 | 学生自主练习与查分(含口语转写契约) | ✓ | API 全链路测试通过 |
| M4 | 教师复核 + 双评仲裁 + 审计 | ✓ | 双评取均值/仲裁用例通过；审计不可删；双评同分漏计bug已按reviewed_by字段修复并新增4个边界测试，数据库1组演示双评完成对(grader/grader2均给4.0分) |
| M5 | 能力诊断(Q 矩阵/BKT/画像) + 报告 | ✓ | 60 维报告 + BKT 时序 + profile_match 用例通过(60维为结构数量，非诊断匹配度已达标) |
| M6 | 质量看板(重放一致率/双评一致/引擎时延/校准) | ✓ | 看板接口实读有数(见第三节)；指标已更名"存量分数重放一致率"；时延为引擎内部处理时间，非端到端 |
| M7 | 图片 OCR 上传与识别(代码链路) | ✓ | 视觉模型适配代码就绪(智谱glm-4v-flash)；未配置时回退本地RapidOCR；**真实OCR效果已独立验证(见M7a)** |
| M7a | OCR 真实效果验证(CER) | ✓ | **2026-09-13 完成**：有效Key+glm-4v-flash，4张真实手写图全部识别成功，平均CER 21.91%/加权24.06%；glm-4.7纯文本跑通但无视觉，OCR用glm-4v-flash；真值、脚本、报告齐全 |
| M7b | 本地 OCR 离线兜底 | ✓ | **已安装 rapidocr-onnxruntime 1.4.4**，import 验证通过，未配置 Key 时自动回退离线识别 |
| M7c | ASR 转写 + 人工真值集(WER/CER) | ▲ | faster-whisper==1.2.1等依赖已锁定，模型revision/权重SHA256已提供；**2026-09-13 用4段真实学生录音+1段视频音轨完成真实转写并判分，建立"AI校对代理真值"(WER 16.95%/CER 7.29%)**；按口音/噪声/时长维度的**人工抽检校准(≥10%)**待安排人力和时间点 |
| M8 | 实操视频(基础版) | ✓ | 音轨转写+关键词步骤判分；无关键词步骤转人工 |
| M9 | 前端五角色工作台 + 演示数据 | ✓ | `npm run build` 通过(2254模块，2个构建块>1MB)；一键 `start_all.bat` |
| M10 | 自动化测试 + 交付文档 | ✓ | 91 passed；本文档 + README |
| M11 | .env 敏感密钥清理 | ✓ | **2026-09-13 交付版复核完成**：`.env`真实智谱 Key 已移除为占位模板，`.env.example`已补全 OCR 配置段；全盘扫描（含隐藏文件/git 历史/backend.zip）确认无真实密钥残留（详见风险点8与交付版文档） |
| M12 | 4份异常学生标识迁移 | ✓ | 2026-09-11将answers/scores中4条student_li记录迁移为正式token stu_A7k2，迁移前已备份数据库，异常token记录数由8降为0 |
| M13 | 模拟教师二审测试(关键字对照法) | ✓ | 2026-09-13编写3道基础题+9份三档答案；v1(松散关键字)±1分一致率88.9%(8/9)；v2语义槽算法优化后**100%(9/9)、平均分差0.33**，已落地到引擎评分逻辑(`slot_coverage_ratio`)，91项测试无回归；非真实教师评分，仅作基线 |
| — | **教师盲评实验(真实教师)** | ▲ | 实验方案已提交；**责任人待定，需项目负责人协调任课教师参与**；目标±1分一致率≥90%；当前已有模拟教师二审基线100% |
| — | **能力诊断信效度验证** | ▲ | 实验方案已提交；**责任人待定，需指定内部责任人和时间点** |
| — | **Q 矩阵标注一致性** | ▲ | 实验方案已提交(当前Q矩阵为命题教师单方标注，需两位标注者独立标注后计算Kappa)；**责任人待定** |
| — | **IRT/BKT 大样本标定** | ▲ | 实验方案已提交(当前为题目启发式参数+已观测序列拟合)；**责任人待定** |
| — | **真实教师评分数据** | ▲ | 现有8份已终审为开发环境预置账号操作，非真实教师评分；**真实教师盲评尚未开始** |
| M14 | 端到端多模态评阅验证(OCR/ASR/视频) | ✓ | **2026-09-13 用真实学生素材跑通全链路**：2张图片OCR→判分、4段语音ASR→判分、1个视频音轨→判分；同学2(90分)图片题满分8/8、语音满分6/6，同学1(70分)基本符合；91项测试无回归 |
| — | 端到端压测(含HTTP/上传/ASR/OCR/持久化) | ✓ | **2026-09-13 文本提交端到端压测：avg 16.9ms / p95 19.7ms**（引擎内部0.02ms，占比0.1%）；SQLite WAL已启用；请求耗时中间件已加(X-Response-Time-ms)；ASR/OCR路径随媒体素材实测 |
| — | 实操动作识别 / 音素级发音评测 / 大规模 IRT 标定 | ▲ | 姿态识别与发音对齐未接入，如实上报为后续规划 |

---

## 七、风险点与后续规划(如实上报)

1. **无真实教师评分数据**：现有8份已终审卷均为开发环境预置账号(admin/grader/grader2于2026-09-09)操作记录，final_score与引擎初评一致系种子脚本赋值关系，非真实教师盲评结果；由此得到的"一致率100%"和校准参数(a=1/b=0/r²=1)不具备统计意义，已从正文中删除并如实标注。**真实教师盲评【尚未开始，NOT_RUN】，需项目负责人协调任课教师参与。**
2. **客观题测试集未独立隔离**：47个客观题金标准样例全绿，但样例由开发团队编写，未与独立冻结的真实题库和人工答案隔离；看板"存量分数重放一致率"(30/30)为同一引擎对自身历史评分的重放对账，非独立评测结果。独立评测【尚未开始，NOT_RUN】。
3. **时延口径需收窄**：库存avg 0.12ms / p95 0.48ms / max 1.04ms为引擎内部处理时间(分发前→数据库写入前)，**不含**HTTP、文件上传、ASR转写、OCR识别、外部模型调用、持久化和并发排队，不能表述为系统端到端响应时间。端到端时延待真机压测。
4. **演示库非真实学生数据**：50份答卷中38份由种子脚本自动生成，4份已从可读token迁移为正式token；演示库可用于检查功能链路，不能作为真实学生效果数据集。
5. **OCR真实效果已验证(2026-09-13)**：智谱 Key 已更新有效，glm-4.7 纯文本跑通但无视觉能力，OCR 改用 glm-4v-flash；4 张真实学生手写图片全部识别成功，平均 CER 21.91%/加权 24.06%，正文识别良好、公式域有符号误差，评估脚本与真值已落地 `scripts/ocr_eval/`。
6. **ASR 已有量化基线但真值为AI校对代理**：faster-whisper版本/revision/权重哈希已锁定；2026-09-13 用4段真实学生录音+1段视频音轨建立"AI校对代理真值"，**平均 WER 16.95% / CER 7.29%**（正文同音字为主、公式符号误识别已实证影响判分），脚本与真值见 `scripts/asr_eval/`；**正式交付需人工抽检校准 ≥10% 样本**，按口音/噪声/时长分组的人工真值集【待安排，NOT_RUN】。
7. **四项实验方案责任人待定**：教师盲评、能力诊断信效度、Q矩阵标注一致性、IRT/BKT大样本标定的实验方案(含数据来源与材料清单)已提交，但责任人均未指定。教师盲评需项目负责人协调任课教师参与，其余三项需指定内部责任人和时间点。
8. **.env 敏感密钥已彻底清理（2026-09-13 交付版复核）**：根目录`.env`真实智谱 Key 已移除并替换为占位模板、`.env.example`已补全OCR配置段；已做全盘扫描（含隐藏文件、git 历史、backend.zip 旧快照）确认无真实密钥残留；SECRET_KEY 为开发占位符，生产部署须通过环境变量注入强随机密钥。此问题与并发平台模块的密钥问题同优先级，**交付版已解除**。
9. **前端构建块已优化（2026-09-13 已完成）**：前端生产构建原 2 个块超 1MB，已通过 ECharts 按需引入(Bar/Radar/Heatmap)、manualChunks 拆 vendor-vue/vendor-element、图标按需注册(21个)优化，**当前所有构建块 <1MB**；`npm run build` 通过。
10. **前端依赖漏洞(低优先级，已评估暂缓)**：1个高危(vite≤6.4.2)、2个中危(esbuild≤0.24.2、echarts<6.1.0 XSS)；升级需vite 5→8、echarts 5→6两处破坏性变更，团队评估交付阶段收益小于风险，暂缓处理。vite/esbuild属开发构建期工具不进入运行时，echarts数据来自服务端可信JSON实际利用面低；未升级前提下91项测试保持通过。
11. **口语发音层**：音素级评测源未接入，如实标nodata、不产生无效评分；流畅层已接faster-whisper词级时间戳真算(口语/视频音轨均实测)。
12. **实操视频为"基础版"**：仅内容覆盖维自动判分，动作/时序步骤由教师人工补齐(强制复核兜底)，姿态识别列入后续。
13. **判分模型已配置国产大模型（zhipu + glm-4.7）**：2026-09-13 用户提供有效智谱 Key 后完成实测——glm-4.7 纯文本调用 HTTP 200（判点复核+评语），glm-4v-flash 视觉调用 HTTP 200（OCR）；glm-4.7 不支持图片输入，故双模型分工。**交付版已移除真实 Key**：未配置 Key 时自动回退本地内置确定性引擎（功能完整可演示），配置 `GRADE_LLM_*` 即升级为「规则定分 + 大模型复核」协同模式。
14. **后续规划**：生产部署加固(多worker + 幂等锁)、并发批卷、REST文档化开放接口、真实题库IRT/BKT参数标定、端到端压测。

---
*报告数据均可复现：后端 `cd backend && .venv\Scripts\python -m pytest -q`(91 passed)；前端 `cd frontend && npm run build`；演示库看板通过 `admin` 登录教务看板读取。*
